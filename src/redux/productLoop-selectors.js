export const getProductLoopItemsState = (state) => {
  return state.productLoop.productLoopItems;
}

export const getProductLoopLoadingState = (state) => {
  return state.productLoop.productLoopLoading;
}